#include "corewar.h"

int ft_zjmp(t_game *game, t_carriage *car)
{
    if (game == NULL || car == NULL)
        ft_error("error");
    
    //init и поиск байтов для параметров

    //выполнить 
    return (0);
}